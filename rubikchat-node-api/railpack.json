{
  "packages": {
    "nodejs": "22"
  },
  "build": {
    "steps": [
      "npm install",
      "npx prisma generate",
      "npx tsc"
    ]
  },
  "deploy": {
    "start": "npx prisma db push --accept-data-loss && node dist/index.js"
  }
}
