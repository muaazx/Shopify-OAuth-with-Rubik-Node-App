{
  "packages": {
    "nodejs": "22"
  },
  "build": {
    "steps": [
      "npm install",
      "npx prisma generate",
      "npx tsc"
    ]
  },
  "deploy": {
    "start": "npx tsc && npx prisma migrate deploy && node dist/index.js"
  }
}
